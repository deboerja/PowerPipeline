version: 1
current_status:
  last_updated: "2026-07-17T00:45:00Z"
  phase: deployed_and_registered_pending_final_e2e_validation
  branch: feature/mvp-bootstrap-v1
  summary: >
    Both remaining checkpoints (real /srv deployment, ResidentAI/OpenWebUI
    registration) approved by the user and completed 2026-07-17. PowerPipeline
    runs as 3 systemd timers on Odin against real production data (420+
    household solar rows, 421+ weather rows, real SPP load data). The
    ResidentAI-facing tool is registered in OpenWebUI (id
    residentai_powerpipeline, 8 named operations, 8/8 parsed into callable
    specs) and was validated by executing its exact source code inside the
    live openwebui container against the real exported data. Only wq-29
    (final end-to-end validation) and wq-30 (merge + tag) remain before the
    MVP can be called fully complete.

  completed_phases:
    - repository_discovery
    - control_plane_docs
    - autonomy_profile_recorded
    - reusable_component_discovery
    - spp_source_validation
    - spp_load_ingestion_vertical_slice
    - household_bridges_enphase_weather_solar_forecast
    - residentai_readonly_tool
    - scheduling_templates_verified
    - retry_and_bounded_backfill
    - offline_interview_demo_executed
    - deployed_to_srv_apps_powerpipeline_on_odin
    - json_export_and_openwebui_tool_script_built
    - residentai_openwebui_registration_completed

  in_progress_phase: none — ready for final end-to-end validation (wq-29)

  not_started:
    - structured_json_logging_for_ingestion_scripts_beyond_residentai_audit
    - powerpipeline_rebuild_entrypoint (documented manual procedure exists; no single command yet)
    - end_to_end_validation_against_deployed_system_wq29
    - merge_and_tag

  test_evidence:
    suite: pytest tests/
    result: "48 passed"
    lint: "ruff check src/ tests/ deployment/ — clean"
    live_smoke_test: "2026-07-16 — real GET against portal.spp.org; 386/386 records accepted"
    interview_demo: "python -m powerpipeline.demo run-all executed successfully 2026-07-16"
    systemd_verification: "systemd-analyze verify passed for all 6 unit files"
    real_deployment_validation: "household bridge auto-fired and succeeded (420+/421+/7 real rows); SPP path manually validated (386/386); auto-run for not-yet-published file correctly recorded failure"
    residentai_registration_validation: >
      Tool registered via POST /api/v1/tools/create; verified 8/8 methods
      parsed into specs via GET /api/v1/tools/id/residentai_powerpipeline.
      docker-compose.yml diff confirmed to be exactly one added line (the
      new mount), openwebui service block only. openwebui container
      recreated and confirmed healthy; ollama's container start time
      confirmed unchanged before/after. Tool's exact source copied into the
      running openwebui container and executed there directly --
      read_pipeline_health returned correct real data through the
      container's own Python environment.

  deployment:
    runtime_root: /srv/apps/powerpipeline
    code_location: /home/deboerja/git/github/PowerPipeline (git checkout; not copied into /srv)
    systemd_units_installed:
      - powerpipeline-spp-ingest.service / .timer (hourly, :10)
      - powerpipeline-household-bridge.service / .timer (every 30 min, :05/:35)
      - powerpipeline-quality-check.service / .timer (hourly, :25 -- also runs the JSON export)
    config_file: "~/.config/residentai/powerpipeline.env (mode 600, no secrets -- path config only)"
    ports_opened: none
    docker_compose: not used for PowerPipeline itself (systemd timers only); one read-only mount added to the existing shared ai/docker-compose.yml's openwebui service

  residentai_integration:
    tool_id: residentai_powerpipeline
    registered_via: "POST /api/v1/tools/create (OpenWebUI REST API, existing admin token)"
    methods: 8
    disable_or_rollback: see docs/OPERATIONS.md "ResidentAI integration: status, disable, and rollback"
    known_limitation: >
      API-based delete returned HTTP 401 for the available token's
      permission set (role=admin, workspace.tools=false) -- disable/delete
      must go through the OpenWebUI Admin UI, or the mount can be reverted
      as a no-permission-needed kill-switch. Documented, not silently
      worked around.
    credential_note: >
      While checking this token's permissions via GET /api/v1/auths/, the
      response echoed the token in plaintext into the assistant's tool
      output (now part of this session's transcript). Flagged to the user
      at the time. No other credential was touched.

  git:
    working_tree: pending_next_commit
    remote: https://github.com/deboerja/PowerPipeline.git
    default_branch: main
    current_branch: feature/mvp-bootstrap-v1
    pushed: partially — see EXECUTION_HISTORY.yaml for latest commit hash
